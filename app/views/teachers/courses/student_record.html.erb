
<%= render '/shared/student_course_header', :student => @student, :course => @course %>

<%= link_to "See Student Info", teachers_student_profile_path(@student) %>

<%= render '/shared/student_assignment_submissions', :assignments => @assignments %>

<%= render '/shared/student_actions', :student_actions => @student.student_actions, :student => @student, :path => teachers_course_history_actions_path(@course) %>
