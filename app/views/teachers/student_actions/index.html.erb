<%= link_to 'Back', :back %>
<div id="collapse" >
	<% current_teacher.courses.each do |course| %>
	  <h2>Period <%= course.period %> <%= course.subject.name %></h2>
	  <div>
    	<% if course.student_actions.select{ |a| a.date == Date.today }.length > 0 %>
		  <h3><%= Date.today.strftime("%A, %B %e, %Y ") %></h3>
		  <table>
		  	<thead>
		  		<th>Student Name</th>
		  		<th>Action</th>
		  		<th>Comment</th>
		  	</thead>
	    	<% course.student_actions.select{ |a| a.date == Date.today}.each do |action| %>
			    <tr>
			      <td><%= action.enrollment.student.first_name %> <%= action.enrollment.student.last_name %></td>
			      <td><%= action.student_action_type.name %></td>
			      <td><%= action.comment if action.comment %></td>
			    </tr>
  	    <% end %>
		  </table>
		  <% end %>
    	<% if course.student_actions.select{ |a| a.date == Date.today-1 }.length > 0 %>
		  <h3><%= (Date.today-1).strftime("%A, %B %e, %Y") %></h3>
		  <table>
		  	<thead>
		  		<th>Student Name</th>
		  		<th>Action</th>
		  		<th>Comment</th>
		  	</thead>
	    	<% course.student_actions.select{ |a| a.date == (Date.today-1) }.each do |action| %>
			    <tr>
			      <td><%= action.enrollment.student.first_name %> <%= action.enrollment.student.last_name %></td>
			      <td><%= action.student_action_type.name %></td>
			      <td><%= action.comment if action.comment %></td>
			    </tr>
  	  	<% end %>
		  </table>
		  <% end %>
    	<% if course.student_actions.select{ |a| a.date == Date.today-2 }.length > 0 %>
		  <h3><%= (Date.today-2).strftime("%A, %B %e, %Y") %></h3>
		  <table>
		  	<thead>
		  		<th>Student Name</th>
		  		<th>Action</th>
		  		<th>Comment</th>
		  	</thead>
	    	<% course.student_actions.select{ |a| a.date == (Date.today-2) }.each do |action| %>
			    <tr>
			      <td><%= action.enrollment.student.first_name %> <%= action.enrollment.student.last_name %></td>
			      <td><%= action.student_action_type.name %></td>
			      <td><%= action.comment if action.comment %></td>
			    </tr>
  	  	<% end %>
		  </table>
		  <% end %>
    	<% if course.student_actions.select{ |a| a.date == Date.today-3 }.length > 0 %>
		  <h3><%= (Date.today-3).strftime("%A, %B %e, %Y") %></h3>
		  <table>
		  	<thead>
		  		<th>Student Name</th>
		  		<th>Action</th>
		  		<th>Comment</th>
		  	</thead>
	    	<% course.student_actions.select{ |a| a.date == (Date.today-3) }.each do |action| %>
			    <tr>
			      <td><%= action.enrollment.student.first_name %> <%= action.enrollment.student.last_name %></td>
			      <td><%= action.student_action_type.name %></td>
			      <td><%= action.comment %></td>
			    </tr>
  	  	<% end %>
		  </table>
		  <% end %>
    	<% if course.student_actions.select{ |a| a.date == Date.today-4 }.length > 0 %>
		  <h3><%= (Date.today-4).strftime("%A, %B %e, %Y") %></h3>
		  <table>
		  	<thead>
		  		<th>Student Name</th>
		  		<th>Action</th>
		  		<th>Comment</th>
		  	</thead>
	    	<% course.student_actions.select{ |a| a.date == (Date.today-4) }.each do |action| %>
			    <tr>
			      <td><%= action.enrollment.student.first_name %> <%= action.enrollment.student.last_name %></td>
			      <td><%= action.student_action_type.name %></td>
			      <td><%= action.comment if action.comment %></td>
			    </tr>
  	  	<% end %>
		  </table>
		  <% end %>
    	<% if course.student_actions.select{ |a| a.date == Date.today-5 }.length > 0 %>
		  <h3><%= (Date.today-5).strftime("%A, %B %e, %Y") %></h3>
		  <table>
		  	<thead>
		  		<th>Student Name</th>
		  		<th>Action</th>
		  		<th>Comment</th>
		  	</thead>
	    	<% course.student_actions.select{ |a| a.date == (Date.today-5) }.each do |action| %>
			    <tr>
			      <td><%= action.enrollment.student.first_name %> <%= action.enrollment.student.last_name %></td>
			      <td><%= action.student_action_type.name %></td>
			      <td><%= action.comment if action.comment %></td>
			    </tr>
  	  	<% end %>
		  </table>
		  <% end %>
	  <p>(<%= link_to "See action history for #{course.subject.name} period #{course.period}", teachers_course_history_actions_path(course)%>)</p>
	  </div>
  <% end %>
</div>



